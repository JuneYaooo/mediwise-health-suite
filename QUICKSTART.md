# MediWise 快速开始

> [返回 README](README.md) · [查看文档中心](docs/README.md)

MediWise Health Suite 是面向支持 Skills 的 AI Agent 的个人本地健康助手，可运行在 Hermes、OpenClaw、Claude Code、Codex、WorkBuddy，以及其他能够加载 Skills、访问本地文件并执行脚本的 Agent 工具中。一个本地用户可以管理自己的档案，也可以代管多位家人的档案；它不是群聊机器人或多人共享健康服务。

## 1. 让 AI 完成安装

把下面这段话发给 Hermes、OpenClaw、Claude Code、Codex、WorkBuddy，或其他具备本机终端与网络权限的 AI 助手：

```text
请帮我安装并配置 MediWise Health Suite：
https://github.com/JuneYaooo/mediwise-health-suite
完成后帮我检查 Skill 是否已正确加载并可以正常使用。
```

仓库里已经写好了给安装 Agent 阅读的完整步骤。AI 会负责选择正确目录、安装依赖、启用个人模式并执行基础验收。普通用户不需要复制终端命令，也不需要自己修改配置文件。

## 2. 创建第一个档案

安装并让当前 Agent 重新加载 Skills 后，直接说：

```text
帮我创建本人档案，我叫林安。
```

也可以补充生日、性别、过敏史等信息。敏感信息只提供健康管理所必需的最少内容。

成员规则：

- 只有一个“本人”档案时，未指定姓名的记录默认属于本人。
- 添加第二位成员后，写入健康数据必须明确姓名。
- 助手会用“姓名（身份）”确认目标，例如“林安（本人）”“张建国（父亲）”。
- 同名成员必须同时说明身份，助手不能猜。

添加家人可以说：

```text
帮我添加一位家庭成员：张建国，是我的父亲，65 岁。
```

## 3. 开始记录

```text
记录我今天早上血压 124/79，心率 71。
帮张建国记录今天晚饭后血糖 7.2 mmol/L。
我今天开始服用维生素 D，每日一次，每次 1 粒。
记录今天体重 62.8 kg。
记录今天午餐：米饭 150g、鸡胸肉 120g、青菜 200g。
```

饮食中的热量和营养值不会由模型凭记忆直接估算。默认未配置食物数据源时，助手会请你提供包装营养标签，或在取得同意后由配置 Agent 启用 USDA、Open Food Facts 或已授权的本地数据包。

涉及多人时，助手应先复述最终目标成员；图片识别出的内容、异常单位或不确定数据，也必须经确认后才写入。

## 4. 查看健康记录卡片

```text
帮我生成最近 7 天的健康记录卡片。
```

只有一个本人档案时会默认本人。存在多位成员时请明确姓名：

```text
帮我生成张建国最近 30 天的健康记录卡片。
```

卡片包含实际已有的指标、记录次数、趋势、来源、提醒和在用药。没有记录的项目会显示暂无数据，不会为了补全卡片而编造内容。

## 5. 图片和 PDF 识别

如果当前 Agent 能直接读取图片或 PDF，上传附件后直接说即可，不需要为 MediWise 单独配置视觉模型：

```text
[上传一份脱敏报告] 请提取关键内容，先让我确认，再写入档案。
```

Skill 会先使用当前 Agent 已有的附件理解能力，并在写入前让你确认识别结果。如果当前 Agent 无法读取该附件，才需要由具备本机权限的配置 Agent 添加本地 OCR 或可选视觉服务。云端视觉服务会收到完整图片或 PDF 页面，体检报告使用前应脱敏。

## 6. 导入可穿戴数据

当前已验证的文件导入来源：

- Apple Health 导出的 `export.zip` 或 `export.xml`。
- Gadgetbridge 导出的本地 SQLite 数据库。

上传文件后直接说：

```text
请把这个 Apple 健康导出包导入我的 MediWise 档案。导入前先检查格式，完成后告诉我新增、跳过了多少条记录，以及覆盖了哪些指标和时间范围。
```

Garmin Connect 目前是实验性接入；Huawei Health Kit、Zepp 云账号和 OpenWearables 暂不作为可用功能。详见 [可穿戴数据导入指南](docs/WEARABLES.md)。

## 7. 常用对话

```text
帮我看最近 30 天的血压趋势。
我上传一张脱敏化验单，请提取内容，先让我确认再记录。
我下周要看医生，帮我整理近期指标、症状和在用药。
每天晚上 9 点提醒我测量血压。
请为 MediWise 创建完整本地备份，并告诉我保存位置和校验结果。
```

提醒首先保存为本地待办。只有当前 Agent 支持定时任务、已完成任务注册并通过触发测试时，才会到点主动通知；否则可以随时查询未完成提醒。

## 8. 安全边界

- 默认使用本地 SQLite 保存健康数据。
- 不要把同一数据目录用于群聊或多人共享服务。
- 不要在聊天中发送 API Key、账号密码或 token。
- 可选云端视觉、在线食物查询和远程 Embedding 只有显式配置后才会访问外部服务。
- MediWise 只提供记录、整理、查询、展示和提醒，不提供诊断、治疗建议、用药建议、营养治疗建议、临床判断或其他医疗指导。
- 报告中的异常标记、用户设置的阈值和到期提醒只用于信息展示；如需医学判断，请咨询专业医疗人员。

遇到安装或识别问题时，把错误摘要交给具备本机权限的配置 Agent 处理；普通用户不需要自己运行修复命令。完整说明见 [安装指南](docs/INSTALLATION.md)，其他文档见 [文档中心](docs/README.md)。
